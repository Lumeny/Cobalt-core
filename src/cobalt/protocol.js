'use strict';

const p = {
    ACTION: {
        AUTHENTIFCATION: 'authentification',
        PING: 'ping',
        MESSAGE: 'MESSAGE',
        RESPONSE: 'RESPONSE'
    }
};

module.exports = Object.freeze(p);